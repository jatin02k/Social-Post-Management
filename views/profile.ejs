<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini Project</title>
    <script src="https://cdn.tailwindcss.com"></script>

</head>

<body>
    <div class="w-full min-h-screen p-10 bg-zinc-900 text-white">
        <div class="flex w-full justify-end">
            <div class="flex flex-col">
                <a class="text-red-600 inline-block mb-2" href="/upload">edit Porfile pic</a>
            <a class="text-red-600 inline-block mb-2" href="/logout">Logout</a>
            </div>
        </div>
        <div class="w-20 h-20 rounded-md">
            <img class="w-full h-full object-cover overflow-hidden" src="/images/upload/<%= user.profilepic %>" alt="">
        </div>
        <h3 class="text-3xl mb-2">Hello, <%= user.name %>
        </h3>
        <h5 class="mb-3 text-zinc-400">you can create a new post</h5>
        <form action="/post" method="post">
            <textarea class="w-1/3 px-2 block resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                placeholder="write your post here " name="content" id=""></textarea>
            <input class="px-3 py-2 bg-blue-600 block rounded-md mt-2" type="submit" value="Create Post">
        </form>
        <div class="posts mt-5">
            <h3 class="text-zinc-400 mb-3">Your Posts</h3>
            <div class="">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="postcontainer">
                        <div class="post mb-2 w-1/3 p-2 border-[1px] border-zinc-700 bg-zinc-800 rounded-md">
                            <h4 class="text-blue-600 mb-2">@<%=user.username %></h4>
                            <p class="text-sm tracking-tighter"><%= post.content %>
                            </p>
                            <small class="mt-2 inline-block"><%=post.likes.length%>likes</small>
                            <div class="btn flex mt-3 gap-3">
                                <a class="text-blue-500" href="/like/<%=post._id%>">
                                    <%=post.likes.indexOf(user._id)=== -1 ? "like": "unlike" %>
                                </a>
                                <a class="text-zinc-500" href="/edit/<%=post._id%>">Edit</a>
                                <a class="text-red-500" href="/delete/<%=post._id%>">Delete</a>
                            </div>
                        </div>
                    </div>
                <% })%>
            </div>

        </div>


    </div>
</body>

</html>